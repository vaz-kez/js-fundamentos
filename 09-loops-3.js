// Bucles / Loops 'do' 'while'

var i = 0;

while (i <= 10) {
  i++
}
console.log(`La variable i vale ${i}`);

console.log(`--------------------------`)
//////////////////////////////////////////////////////

// El bucle 'do' no se utiliza mucho
var j = 0;   //  Se toma la variable

do {
  j++;      //  Se cambia la cuenta en el punto que se quiera del bucle
} while (j <= 10);    // Y se compara / termina el valor al final 

console.log(`La variable J vale ${j}`)